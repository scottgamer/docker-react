sudo: required
services: 
  - docker

before_install:
  - docker build -t scottgamer/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true scottgamer/docker-react npm run test -- --coverage